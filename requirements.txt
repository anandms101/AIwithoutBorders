# Outpost runtime dependencies — fully pinned.
# The venue has no usable Wi-Fi (AGENTS.md working conventions), so every
# transitive version is locked here. Regenerate with:
#   uv pip compile pyproject.toml -o requirements.txt
# or, after an install:  uv pip freeze
#
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt
# or, preferred on this box:
#   uv venv --python 3.12 && uv pip install -r requirements.txt

# --- Web UI (FastAPI + server-rendered Jinja, no SPA) ---
fastapi==0.121.2
starlette==0.49.3
uvicorn==0.41.0
jinja2==3.1.6
markupsafe==3.0.3
python-multipart==0.0.20

# --- HTTP client (Ollama + egress) ---
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.14.2
certifi==2026.7.22
idna==3.18

# --- Watcher (F-01) ---
watchdog==6.0.0

# --- Shared ---
pydantic==2.13.4
pydantic-core==2.46.4
annotated-types==0.8.0
annotated-doc==0.0.4
typing-extensions==4.16.0
typing-inspection==0.4.2
click==8.4.2
